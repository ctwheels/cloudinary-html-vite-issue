import { Actions } from '@cloudinary/url-gen';
import { Qualifiers } from '@cloudinary/url-gen';
import { lazyload, responsive, accessibility, placeholder } from '@cloudinary/html';

const Plugins = {
	lazyload,
	responsive,
	accessibility,
	placeholder,
};

export { Actions, Qualifiers, Plugins };
